import 'package:flutter/material.dart';

class MagicExistNavBar extends StatelessWidget {
  const MagicExistNavBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Placeholder(fallbackHeight: 50);
  }
}
